---
const { frontmatter } = Astro.props;

import Layout from '../full.astro';
---

<Layout desc={frontmatter.desc} title={frontmatter.title} dynamicElements={frontmatter.dynamicElements}>
    <slot />        
</Layout>