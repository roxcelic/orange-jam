---
interface Props {
	title: string;
    desc: string;
    wallpaper: string;
    dynamicElements: string;
}

const { desc="", title="home", wallpaper="" } = Astro.props;

import Layout from '../full.astro';
---

<Layout desc={desc} title={title} wallpaper={wallpaper}>
    <slot />
</Layout>